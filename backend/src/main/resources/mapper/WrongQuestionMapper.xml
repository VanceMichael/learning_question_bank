<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.questionbank.mapper.WrongQuestionMapper">

    <resultMap id="WrongQuestionResultMap" type="com.questionbank.entity.WrongQuestion">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="question_id" property="questionId"/>
        <result column="subject_id" property="subjectId"/>
        <result column="wrong_count" property="wrongCount"/>
        <result column="last_wrong_at" property="lastWrongAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="subject_name" property="subjectName"/>
        <association property="question" javaType="com.questionbank.entity.Question">
            <id column="q_id" property="id"/>
            <result column="q_subject_id" property="subjectId"/>
            <result column="q_creator_id" property="creatorId"/>
            <result column="q_type" property="type"/>
            <result column="q_content" property="content"/>
            <result column="q_options" property="options" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
            <result column="q_answer" property="answer" typeHandler="com.baomidou.mybatisplus.extension.handlers.JacksonTypeHandler"/>
            <result column="q_analysis" property="analysis"/>
            <result column="q_difficulty" property="difficulty"/>
            <result column="q_status" property="status"/>
            <result column="q_created_at" property="createdAt"/>
            <result column="q_updated_at" property="updatedAt"/>
        </association>
    </resultMap>

    <select id="selectByUserAndSubject" resultMap="WrongQuestionResultMap">
        SELECT wq.*,
               q.id as q_id,
               q.subject_id as q_subject_id,
               q.creator_id as q_creator_id,
               q.type as q_type,
               q.content as q_content,
               q.options as q_options,
               q.answer as q_answer,
               q.analysis as q_analysis,
               q.difficulty as q_difficulty,
               q.status as q_status,
               q.created_at as q_created_at,
               q.updated_at as q_updated_at,
               s.name as subject_name
        FROM wrong_question wq
        LEFT JOIN question q ON wq.question_id = q.id
        LEFT JOIN subject s ON wq.subject_id = s.id
        WHERE wq.user_id = #{userId}
        <if test="subjectId != null">
            AND wq.subject_id = #{subjectId}
        </if>
        ORDER BY wq.last_wrong_at DESC
    </select>

    <select id="selectByUser" resultMap="WrongQuestionResultMap">
        SELECT wq.*,
               q.id as q_id,
               q.subject_id as q_subject_id,
               q.creator_id as q_creator_id,
               q.type as q_type,
               q.content as q_content,
               q.options as q_options,
               q.answer as q_answer,
               q.analysis as q_analysis,
               q.difficulty as q_difficulty,
               q.status as q_status,
               q.created_at as q_created_at,
               q.updated_at as q_updated_at,
               s.name as subject_name
        FROM wrong_question wq
        LEFT JOIN question q ON wq.question_id = q.id
        LEFT JOIN subject s ON wq.subject_id = s.id
        WHERE wq.user_id = #{userId}
        ORDER BY wq.last_wrong_at DESC
    </select>

</mapper>
